// let arr = [];
// const a = [{ id: 1 }, { id: 2 }];
// const b = [{ id: 3 }, { id: 4 }];

// var resultA = arr.some((obj) => {
//   return obj.id === a[0].id;
// });
// var resultB = arr.some((obj) => {
//   return obj.id === b[0].id;
// });

// if (!resultA) arr = arr.concat(a);
// if (!resultB) arr = arr.concat(b);

// // console.log(arr);
// function addObjectArayNotRepeat(arr=[],arrObject=[])

function check(number) {
  const num = number.toString();
  let check = false;
  num.forEach((element) => {});

  return number < 5;
}
for (let i = 0; i < 999; i++) {
  if (check(i)) console.log({ i: i });
}
