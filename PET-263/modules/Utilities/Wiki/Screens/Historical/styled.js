import styled from 'styled-components/native'
import AutoHeightWebView from 'react-native-autoheight-webview'

export const Wrapper = styled.ScrollView`
  flex: 1;
`
export const WebView = styled(AutoHeightWebView)`
  flex: 1;
`
